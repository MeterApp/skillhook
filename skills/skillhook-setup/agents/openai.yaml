interface:
  display_name: "skillhook Setup"
  short_description: "Install and expose a webhook endpoint that runs agent skills"
  default_prompt: "Use $skillhook-setup to install skillhook on this machine, expose it and verify a webhook end to end."
policy:
  allow_implicit_invocation: true
